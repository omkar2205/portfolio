const front=document.querySelector('[data-chapter="front"]');const back=document.querySelector('[data-chapter="back"]');const flow=document.getElementById('text-flow');const frontStates=[...document.querySelectorAll('[data-copy-step]')];const backStates=[...document.querySelectorAll('[data-back-step]')];const frontCounter=document.getElementById('front-step-counter');const backCounter=document.getElementById('back-step-counter');const frontRail=document.getElementById('front-rail');const backRail=document.getElementById('back-rail');const progressBar=document.getElementById('page-progress-bar');const frontPortrait=document.querySelector('.front-core img');const backPortrait=document.querySelector('.back-core img');const navLinks=[...document.querySelectorAll('[data-nav]')];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const clamp=(n,min=0,max=1)=>Math.min(Math.max(n,min),max);const compact=()=>innerWidth<=900;function chapterProgress(section){if(!section)return 0;const rect=section.getBoundingClientRect();const range=Math.max(section.offsetHeight-innerHeight,1);return clamp(-rect.top/range)}function activate(states,index){states.forEach((el,i)=>el.classList.toggle('is-active',i===index))}function updateFront(){if(!front||compact())return;const p=chapterProgress(front);const step=Math.min(2,Math.floor(clamp(p,0,.9999)*3));activate(frontStates,step);front.dataset.activeStep=step;if(frontCounter)frontCounter.textContent=String(step+1).padStart(2,'0');if(frontRail)frontRail.style.transform=`scaleX(${p})`;if(!reduced&&frontPortrait){const w=(p-.5)*2;frontPortrait.style.transform=`translate3d(0,${w*5}px,0) rotateY(${w*2.2}deg)`}if(flow){const local=clamp((p-2/3)/(1/3));flow.classList.toggle('is-visible',step===2);const e=1-Math.pow(1-local,3);flow.style.setProperty('--flow-x-far',`${-72+e*95}vw`);flow.style.setProperty('--flow-x-mid',`${-92+e*125}vw`);flow.style.setProperty('--flow-x-near',`${-112+e*155}vw`)}}function updateBack(){if(!back||compact())return;const p=chapterProgress(back);const step=Math.min(1,Math.floor(clamp(p,0,.9999)*2));activate(backStates,step);if(backCounter)backCounter.textContent=String(step+1).padStart(2,'0');if(backRail)backRail.style.transform=`scaleX(${p})`;if(!reduced&&backPortrait){const w=(p-.5)*2;backPortrait.style.transform=`translate3d(0,${w*-5}px,0) rotateY(${w*-2}deg)`}}function updatePage(){if(progressBar){const max=Math.max(document.documentElement.scrollHeight-innerHeight,1);progressBar.style.transform=`scaleX(${clamp(scrollY/max)})`}const marker=innerHeight*.35;let active='about';['about','experience','contact'].forEach(id=>{const s=document.getElementById(id);if(s&&s.getBoundingClientRect().top<=marker)active=id});navLinks.forEach(a=>a.classList.toggle('is-active',a.dataset.nav===active))}let ticking=false;function render(){ticking=false;updatePage();updateFront();updateBack()}function requestRender(){if(ticking)return;ticking=true;requestAnimationFrame(render)}addEventListener('scroll',requestRender,{passive:true});addEventListener('resize',requestRender);render();